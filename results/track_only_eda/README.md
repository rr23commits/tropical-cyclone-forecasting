# Track-only EDA

All feature and target distribution summaries use training origins only. The three displayed storms are deterministic training-only length quantiles; they are illustrations, not selected for model performance. `data_counts.csv` and `missingness.csv` document raw-data eligibility before the frozen forecasting split.
