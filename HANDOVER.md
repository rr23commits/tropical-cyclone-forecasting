# Handover

## Current state

This is a B.Tech tropical-cyclone track and wind forecasting project. The finalized primary result is the reproducible IBTrACS v04r01 North Atlantic, HURDAT-aligned, track-only experiment: five six-hour history states, direct +6/+12/+24/+48-hour track/wind forecasts, train <=2015, validation 2016--2019, and held-out 2020--2025 storms. Its source-data reproduction, held-out error analysis, report, and replay were regenerated on 2026-09-16 and agree with the archived metrics. Genesis ERA5 remains a separate incomplete extension: 899 validated outputs, 5 persisted CDS jobs, 20 no-ID delivery-unknown entries, and 1,868 no-ID DNS-resolution failures. Its 374 complete candidates provide 370 train / 4 validation / 0 test histories, so no ERA5 feature extraction, model, or reported environmental result is authorized. Satellite data is not implemented.

The Phase 4-derived union has 2,472 issue dates and 10,867 distinct `(SID, issue_time)` origins. The prior ERA5 September--October plan and all of its state remain preserved as historical work, but it is explicitly out of scope for the current deliverable.

Genesis new-submission transport uses a fresh Requests session, 10-second connect and 90-second read timeouts, and `retry_max=1`. On resume, all no-ID non-DNS ambiguities are matched against one bounded paginated CDS history snapshot by exact canonical payload before any POST: exactly one match is persisted/recovered; zero or multiple matches stay unresolved and are never resubmitted. A history-read/network failure leaves them unresolved and continues normal resume without a POST. Only positively identified pre-CDS DNS failures are eligible for retry, with the previous attempt retained in `submission_history`. Duplicate CDS job IDs are rejected. Phase 4 behavior is unchanged. The earlier controlled 20-request transport test stopped on DNS failure before CDS; no acquisition has run after this snapshot fix. Manifest SHA-256 remains `9a4add829832a428049c6f8e75d9a70a40fdff5bec05783033f28f9ca19ff4e3`.

The original July pressure-wind job `e6585a2b-5393-43a2-b553-b29507b0af8d` was resumed without resubmission and is locally validated. A one-time operator recovery archived the provenance-less legacy 502 record for `sst_198511.nc`, then submitted the current manifest-derived request once as CDS job `a1d556f5-df45-460e-a592-86a21eb44f13`; its 3,173,019-byte result validated and was atomically promoted. The September 1981 pressure-wind job `ad689dd9-00e7-4e14-a4ad-fcf187331f38` was recovered by the curl path: it resumed from byte 90,238,720, reached its CDS-reported 137,340,043 bytes, validated its 100 manifest timestamps and 0.25° grid, and atomically promoted without resubmission. The June 1981 pressure-wind file was manually recovered but its journal entry remains `submitted` until the pipeline validates it. `src.acquire_era5` holds an exclusive process lock, journals every request ID, and, once a recorded job is successful, gets its CDS results asset URL and expected byte count then runs system `curl` against the same job-specific `.part` file. Curl resumes with `--continue-at -`, retries up to three times, and has 900-second per-transfer and retry-window limits. The parent requires byte-count verification, NetCDF validation, then atomic promotion. Curl failure is journaled as `download_error` and exits non-zero; no replacement job is submitted. `Results.download()` and urllib are not used for transfers. The existing June 1981 pressure-wind job `a20b40c6-ab81-495b-b08c-7a6fed63afc1` remains recovery-only; no feature table exists. The durable journal is `data/raw/era5/v1/request_jobs.json` (ignored by Git).

## Working and verified

- Genesis recovery is verified by 21 focused mocked acquisition tests, including one bounded shared history traversal and ReadTimeout fail-closed handling. `make test` and `git diff --check` pass; no live CDS request ran and no journal/manifest/raw data was changed by the implementation.
- Final track-only audit: source re-runs reproduce the stored model outputs; `held_out_errors.csv` has 24,052 unique `(model, SID, issue_time, target_time, horizon)` rows, with identical model origins at each horizon. Evaluation tables distinguish origin-weighted means from storm-macro bootstrap summaries; paired GRU--Ridge inference resamples whole storms. The frontend replay regenerated from these rows contains 107 held-out storms and remains non-operational.
- Static research-replay frontend: `make serve`, then open `http://127.0.0.1:8000`. It is a local-only, non-operational playback of 107 held-out 2020--2025 IBTrACS storms. The continuous slider/playback uses every exported observed timestamp; forecasts appear only when archived results exist for that timestamp. `make frontend-data` regenerates `frontend/data/replay.json` from existing held-out errors plus real IBTrACS display identity fields; it does not train or alter an experiment.
- The frontend bundles `frontend/data/countries-110m.json`, a compact Natural Earth-derived country/coast boundary dataset, for the local pan/zoom SVG basemap. It has no runtime map/data dependency.
- Sidebar collapse now moves each panel completely past its screen edge. A distinct, screen-edge-only tab reopens that panel; left and right states are independent.
- The full-width top header has been removed. The map starts at the viewport top and reserves only the currently open panel widths; collapse sets that side's map margin to zero. `TROP-CYC` is a small left-panel lockup, while local replay/held-out and non-operational status are in the bottom strip.
- Successful frontend initialization replaces the empty loading container with the workstation template. The right panel's visible close control is a viewport-level, panel-edge button because the panel itself scrolls; the screen-edge tab reopens it after collapse.
- `make test` passed 47 tests after the replay-export changes. `node --check frontend/app.js` passed, and the locally served HTML, replay payload, and basemap were fetched successfully.
- `make evaluation-report` reads the existing held-out error exports without retraining and writes `results/track_only_evaluation/`: model×horizon metrics with existing storm-level CIs, difficulty/year tables, paired GRU--Ridge comparisons, and three SVG plots. The current report was generated and `make test` passed 48 tests.
- TCIR feasibility inspection found no temporal overlap with the frozen 2020--2025 held-out set: the documented Atlantic HURDAT2 labels cover 2003--2016. There are 107 held-out storms and 1,774 distinct storm/issue-time pairs in archived errors, but zero eligible TCIR storm/frame matches. No TCIR data was downloaded and no satellite pipeline was added. The public TCIR page documents a per-frame HDF5 `info` group but not its field names, matrix dtype/compression, or file sizes; exact ID/time schema and storage need metadata inspection if a compatible later-period source is selected.
- HURSAT-B1 v07b index audit: among the 94 2020--2024 held-out storms, 80 have a per-storm archive listed (all 2020--2023 storms, 1/15 in 2024); these storms account for 1,327 of 1,586 issue-time origins. This is storm-archive coverage only, not verified image/frame coverage. The index has no per-frame timestamps, so the usable issue-time/sample count remains unknown until the relevant archive manifests are inspected. The 80 archives total 7.50 GB compressed; 2025 is outside v07b. No imagery was downloaded. Do not claim complete-case matches or change the frozen 2020--2025 evaluation based on these counts.
- Full tests: `KMP_DUPLICATE_LIB_OK=TRUE python3 -m unittest discover -s tests -p 'test_*.py' -v` (46 tests passed after the error-analysis addition). These include mocked ERA5 tests only; no CDS request is made.
- Run the Phase 1 audit: `python3 src/audit_phase1.py /path/to/ibtracs.NA.list.v04r01.csv`.
- Run baselines: `python3 -m src.run_baselines /path/to/ibtracs.NA.list.v04r01.csv`.
- Run the final baselines: `KMP_DUPLICATE_LIB_OK=TRUE python3 -m src.run_baselines /path/to/ibtracs.NA.list.v04r01.csv --output results/track_only_baselines.csv`.
- Run the final GRU comparison: `KMP_DUPLICATE_LIB_OK=TRUE python3 -m src.run_gru /path/to/ibtracs.NA.list.v04r01.csv --output results/track_only_gru_comparison.csv`.
- Render the final chart: `python3 -m src.plot_track_results results/track_only_gru_comparison.csv --output results/track_only_track_error.svg`.
- Recreate held-out error analysis: `KMP_DUPLICATE_LIB_OK=TRUE python3 -m src.run_error_analysis /path/to/ibtracs.NA.list.v04r01.csv --output-dir results/track_only_error_analysis`.
- Run history ablation: `python3 -m src.run_history_ablation /path/to/ibtracs.NA.list.v04r01.csv`.
- The raw audit CSV is outside Git at `/private/tmp/ibtracs.NA.list.v04r01.csv` in this environment.

## Important constraints

- Keep only `USA_*` values with `USA_AGENCY == hurdat_atl`; do not mix wind conventions.
- Use exact six-hour states and whole-storm chronological splits; never random row splits.
- Keep raw data out of Git. Do not add environmental or satellite paths without a separately scoped next phase.
- The checked-in virtual environment lacks Torch; use the system `python3` used for the recorded final result.

## Next

After CDS connectivity is confirmed and acquisition is authorized, resume the approved Genesis manifest and durable raw directory at concurrency 2 with `python3 -m src.acquire_era5 --genesis-manifest results/genesis_era5_request_manifest.json --raw-dir data/raw/era5/v1 --concurrency 2`. This will reconcile old non-DNS ambiguities first, recover persisted IDs, reuse validated outputs, and retry only exact recorded DNS-resolution failures. Do not begin feature extraction until every manifest key has a validated output and the mechanical validation audit passes. Track-only reporting artifacts remain in `results/track_only_baselines.csv`, `results/track_only_gru_comparison.csv`, `results/track_only_track_error.svg`, and `results/track_only_error_analysis/`.

## Separate TCC genesis feasibility audit (2026-09-12)

- The updated `src/audit_genesis.py` and focused tests audit only metadata. The run used the public [Zenodo TCC/IBTrACS table](https://doi.org/10.5281/zenodo.19228525) and official global IBTrACS v04r01 CSV, both in `/private/tmp`. No GridSat/ERA5 imagery was downloaded; no model was trained.
- `KMP_DUPLICATE_LIB_OK=TRUE make test` passes all 52 tests, including the focused linkage, tie, single-fix, past-only-input, and boundary checks.
- `source=1` has 57,933 TCC tracks. A shared `number` with `source=2` and a validated 3-hour seam exists for 1,420 tracks; all 1,250 `devflag=1` tracks have this bridge. Position matching then links 1,419 to official IBTrACS SIDs; one single-fix match is excluded. Among linked tracks, 150 have multiple 1,000-km SID candidates; overlap count then nearest distance resolves all 150 with no exact score ties. The remaining 56,514 tracks all have `devflag=0`, the documented lifetime nondevelopment status, and can be negatives only when TCC follow-up covers the label window.
- The full audit manifest and counts by basin/year are `results/genesis_candidate_manifest.csv` (61,622 rows, 10,395 tracks) and `results/genesis_counts_by_basin_year.csv`. Full +24h labels: 839 positive / 60,783 negative. Full +48h: 1,045 positive / 16,442 negative / 44,135 unlabeled. Since the merged table has no basin field for TCC-only tracks, 60,346 rows remain `basin=UNKNOWN`. The conservative basin-resolved cohort excludes them: 1,276 rows, 839/437 +24h positive/negative; train 804 (524/280), validation 141 (89/52), test 331 (226/105). Full cohort split totals are train 36,988 (524/36,464), validation 7,630 (89/7,541), and test 17,004 (226/16,778), positive/negative.
- Decision: **AMBIGUOUS; require a documented basin rule.** The unlinked tracks' labels are supported by `devflag=0` plus follow-up, but their basin cannot be assigned from this merged CSV. The older public v01r01 TCC export lists `initial_ba` for 1982–2009, but has no documented crosswalk to this merged file's IDs and does not cover the full period. Exclude `UNKNOWN` for basin-conditioned or per-basin claims; retain only for an explicitly global experiment that does not use basin as a feature. The exact next blocker before imagery is to verify whether `initial_ba` can be crosswalked for the whole 1982–2018 cohort; if not, record the basin exclusion/global-only rule. Then run metadata-only GridSat frame-time and ERA5 time/grid coverage plus retrieval-volume checks. Do not download imagery until both checks pass.
- Re-run with `KMP_DUPLICATE_LIB_OK=TRUE python3 -m src.audit_genesis /path/to/tcc_ibtracs.csv /path/to/ibtracs.ALL.list.v04r01.csv --output results/genesis_candidate_manifest.csv`; the CLI also refreshes `results/genesis_counts_by_basin_year.csv`.

## Basin-resolved genesis metadata preflight (2026-09-12)

- `src.preflight_genesis` is a metadata-only, fail-closed checker for exactly the existing 1,276 non-`UNKNOWN` manifest rows. It expands the nine 3-hour inputs to 11,484 row-frame references / 6,438 unique timestamps (1982-03-13 03Z to 2018-11-20 03Z), reads only the official NOAA GridSat S3 object index, and emits row-level missing-frame blockers. It does not open NetCDF imagery, request CDS data, train, or alter the frozen experiment.
- The official NOAA S3 mirror has now been fully enumerated and cached in `results/gridsat_s3_index/`; `results/genesis_preflight.json` is the exact pre-download manifest. All 6,438 required GridSat objects exist (0 missing timestamps/channel gaps), all 1,276 candidates have a complete history, and source bytes total **247,888,386,134** (230.86 GiB). The cache is 4 MiB and contains only object keys/byte sizes; no imagery was opened or downloaded.
- **Retrieval blocker:** this workstation has 28 GiB free. Do not start the requested object downloader: even though sequential extraction could reduce peak disk, it would still transfer 230.86 GiB and violates the instruction not to download the full archive in this constrained environment. The compact target representation is fixed for a later adequately provisioned worker: nine 96x96 storm-centred `float16` IR-window patches (about 6.7° square at GridSat's 0.07° grid), ordered t-24h through t and centered only on the TCC position at each input timestamp. Its exact uncompressed tensor payload would be 211,673,088 bytes (201.87 MiB), plus a small candidate/timestamp/label/split manifest. Extraction has not begun, so no final dataset size or invalid-download sample count exists.
- **Remote-subset POC (2026-09-12): STOP.** The official NCEI THREDDS OPeNDAP/NetCDF Subset endpoint timed out before returning even the `.das` metadata for a 1982 frame, so no patch request was made and no imagery transferred. The official S3 object supports byte ranges, but it exposes an opaque NetCDF/HDF5 object (the tested frame is 22,153,501 bytes); byte ranges cannot select a latitude/longitude patch without first reading its HDF5 chunk index, which would be a bespoke raw-file downloader rather than a remote subset service. Do not scale or use S3 range requests. Retry the POC only when NCEI NCSS/OPeNDAP returns metadata and a 96x96 `irwin_cdr` subset demonstrably transfers less than the 22 MB source object.
- Genesis IR alternatives were investigated without downloads. GridSat remains the only source that can preserve all 1,276 rows using global, position-independent exact 3-hour frames. HURSAT-B1 v07b is the global 3-hour, ~8-km storm-centred backup (1978–2024) but public indexes list tar archives, not their member timestamps; inspecting archive members would require separate approval. TC PRIMED/STAR-CIRA is public and global 1987–2023, but IR is attached to opportunistic LEO-overpass files rather than a prescribed 3-hour series, so it cannot provide every nine-frame history. NASA CPC MERGIR offers verifiable 30-minute, ~4-km global IR from 1998 but supports only 820 current rows and requires Earthdata authentication; it is a later-era subset, not this frozen cohort.
- ERA5 catalog metadata confirms hourly global 0.25-degree coverage from 1940 onward; every required timestamp falls within it. The compact request is seven raw fields for five predictors: vorticity@850; u/v@850,200 for shear; RH@700; omega@500; SST. A 5x5-degree (21x21-cell) local patch centered at every history timestamp is spatially valid for all 1,276 positions (latitudes -23.3..36.7; longitude wrap required at ±180) and is about 135.2 MiB uncompressed float32. This is only an estimate; no CDS request was made.
- `KMP_DUPLICATE_LIB_OK=TRUE python3 -m unittest discover -s tests -p 'test_preflight_genesis.py' -v`, focused genesis tests, and `make test` pass (53 total). The focused checker test verifies a missing catalog frame blocks exactly its dependent candidate rows.

## Genesis external-data acquisition status (2026-09-12)

- **GridSat is frozen NO-GO.** The exact NOAA S3 preflight remains the archival record: all 6,438 required frames exist, but acquiring their full objects would transfer 247,888,386,134 bytes (230.86 GiB), beyond the available storage budget. Do not implement, resume, or retry satellite acquisition for this cohort on this workstation. The 1,276 rows, labels, and splits remain unchanged.
- **ERA5-only is also blocked by the available exact-hourly distribution.** The official CDS ARCO pressure-level store is 6-hourly, so it cannot supply the frozen 3-hour sequence. The public hourly ERA5 ARCO mirror was inspected as an explicit distribution alternative (not silently substituted): its chunks are one full global time/37-level field. A representative `u_component_of_wind` chunk is 117,354,372 bytes and SST is 1,334,221 bytes. The six required raw fields (u, v, temperature, specific humidity, vertical velocity, SST) would require global chunks for each of 6,438 unique timestamps, i.e. multi-terabyte transfer before selecting any TCC point. No field values were downloaded.
- Therefore no compact ERA5 feature dataset exists, its final dataset size is **0 bytes**, and the genesis experiment is **not ready for modeling**. Do not create placeholder features or impute/interpolate the missing 3-hour environmental fields. A future worker needs an hourly, location-chunked ERA5 service or an approved CDS request plan that demonstrably transfers only the required point/time values.

## Limited ERA5 geo-chunked POC (2026-09-13)

- A later request tested the specifically documented official geo-chunked ARCO route, not the hourly global-chunk mirror. The pressure store exposes `u/v/r/w` on a 0.25° grid at 00/06/12/18Z; the matching surface store exposes SST at hourly coordinates (its data are updated daily). Only 384 of the accepted 1,276 issue times exactly align to the pressure store; no interpolation or future-time lookup was used.
- For the first eligible TCC row (track 100017, issue 1982-03-15 18Z at -12.2/54.6), the nearest source grid coordinate was -12.25/54.5 and the pressure source timestamp was exact. The first required `u@850` chunk exists (2,548,510 bytes) but its GET timed out after 20.78 seconds; the xarray/Zarr adapter returned NaN fill instead of a value. Stop at this point: no other field values or SST chunks were requested, no feature table was emitted, and no alternate client was used.
- Instrumented complete metadata/coordinate responses were 105,187 bytes (58,880 pressure-store; 46,307 SST-store). The timed-out response's partial body and total bytes across exploratory attempts are not known. The precise blocker and actual store metadata are recorded in `results/genesis_era5_poc_audit.json`. No extraction implementation/tests were added because no reliable extraction path was demonstrated. The cohort, labels, splits, and track-only experiment are unchanged; genesis is not ready for modeling.

## LPS-to-TCC crosswalk investigation (2026-09-13)

- Added `src/audit_lps_tcc_crosswalk.py` as an isolated, metadata-only audit and four synthetic tests. It recovers every one of the nine source=1 TCC positions per accepted manifest row, accepts only same-time unique detected-center matches within 1 degree great-circle distance, and rejects ambiguous or contested matches. It never uses LPS intensity/category as a predictor or label.
- The official LPS Parquet is now local at `/Users/rigelrai/Downloads/lps_v5.4.2-era5-1940-2025-core.parquet` (291,600,496 bytes; 368,925 rows; 276 columns; 1940-05-17 through 2025-12-03). A lazy Polars projection/filter produced a 4,741-row, 464,157-byte CSV containing all eight audit fields at the 6,438 required TCC timestamps; 3,120 timestamps are represented. Pandas has no Parquet engine here, so the existing CLI ran on that exact-time slice. `row_id` repeats across different UTC times in the real schema; validation was narrowed to unique `(time,row_id)` keys, without changing matching rules. The crosswalk found 1,275 zero-match rows, one partial row (5/9), no full 9/9 rows, zero ambiguities, five accepted positions on one LPS track, and 0% full coverage. RH is null throughout the catalogue. Audit outputs: `results/lps_tcc_crosswalk/`.
- Run when the catalogue is available locally: `python3 -m src.audit_lps_tcc_crosswalk --manifest results/genesis_candidate_manifest.csv --tcc-source /private/tmp/tcc_ibtracs.csv --lps-catalogue /path/to/lps_v5.4.2-era5-1940-2025-core.parquet`. Parquet reading requires pyarrow or fastparquet. The tool writes the row/position CSVs, JSON summary, and report under `results/lps_tcc_crosswalk/`.
- Focused tests and `make test` passed (58 tests). No cohort, label, split, preflight, or track-only behavior changed. The crosswalk verdict remains unassigned until real LPS rows are available.

## TCC genesis ERA5 CDS manifest (2026-09-13)

- `src.manifest_genesis_era5` builds and validates a metadata-only manifest from the accepted non-`UNKNOWN` rows and the exact deduplicated source=1 TCC positions. The source manifest has 61,622 rows; the selected cohort remains 1,276 rows / 675 tracks. Histories remain nine 3-hour inputs; 11,484 references deduplicate to 6,676 `(track,time)` crop centers and 6,438 distinct UTC timestamps (1982-03-13 03Z through 2018-11-20 03Z).
- Output: `results/genesis_era5_request_manifest.json` (2,792 requests: 1,396 track×UTC-day pressure and 1,396 SST). Each request has exact CDS dataset/payload, deterministic key, canonical SHA-256, local dynamic area and times. Positions and a frozen cohort/labels/splits fingerprint are embedded. The manifest validates against its source inputs and is atomically written; generation has no CDS submission path.
- Pressure variables/levels preserve the current predictor definition: `u_component_of_wind`, `v_component_of_wind`, `temperature`, `specific_humidity`, `vertical_velocity` × 200/500/700/850 hPa; SST is a separate single-level `sea_surface_temperature` request. The pressure selection includes 13 extra Cartesian variable/level combinations beyond the seven required pairs; it is included in the estimate rather than silently substituted or omitted.
- Every crop center snaps to its nearest 0.25° ERA5 grid point (maximum 0.125° shift); the exact 21×21 crop bounds and dateline wrap are stored per track/time.
- Dynamic 0.25° areas are unions of the 5° crop boxes for each track/day and handle dateline crossing. Boxes span 5–14.75° latitude and 5–16.5° longitude; 22 cross the dateline. The float32 uncompressed request-array estimate is 598,544,352 bytes (570.82 MiB); the deduplicated cropped predictor arrays are estimated at 94,211,712 bytes (89.85 MiB), excluding metadata/compression.
- `KMP_DUPLICATE_LIB_OK=TRUE make test` passes 61 tests. Manifest generation and `--validate-only` passed. No CDS request was submitted; actual download, file validation, crop execution, and feature extraction remain out of scope until a later step.

## TCC genesis ERA5 durable acquisition adapter (2026-09-13)

- `src.acquire_era5 --genesis-manifest results/genesis_era5_request_manifest.json --raw-dir <directory>` loads the approved 2,792 JSON requests as immutable `FileSpec` instances with deterministic `genesis_<product>_<track>_<UTC-day>.nc` targets. It uses the existing coordinator, canonical project lock/journal, bounded workers, curl byte-resume, retry, validation, and atomic promotion. Genesis resume now exact-payload-matches no-ID non-DNS entries before any new POST; unique matches are persisted/recovered, while zero/multiple matches remain unresolved. Only positively identified pre-CDS DNS failures may be retried, preserving prior attempts. Duplicate CDS job IDs are rejected. Phase 4 retains its existing recovery behavior.
- The adapter rejects altered schema, request key/date, dataset, variables, pressure levels, output format, or payload fingerprint before any CDS client is created. Dynamic NetCDF validation checks exact requested timestamps, expected raw variables/units/levels, 0.25° latitude grid, and the circular longitude interval so API dateline output may be signed or 0–360. Files are validated before promotion. The recovery-gap change is covered by focused tests; it has not been run against the existing journal/data.
- The legacy positional CSV Phase-4 acquisition mode remains intact. The genesis command only acquires and validates raw request files; crop/feature extraction is intentionally not implemented. Focused genesis adapter tests cover manifest loading, deterministic identities, pressure/SST separation, dateline geometry, failed-download resume, and ambiguous resume that preserves the journal entry while continuing later requests. `make test` passes 66 tests.
- **Completed live smoke test:** `--smoke-test` selected only track 100015 / 1982-03-13's pressure and SST request. Pressure CDS job `71eda6c8-92dc-4f26-ac11-e31ed5366e52` downloaded and validated as `data/raw/era5/genesis_smoke/v1/genesis_pressure_100015_19820313.nc` (535,572 bytes). SST job `2968a18f-1a93-4866-8189-7b72d5c77f4a` validated as `data/raw/era5/genesis_smoke/v1/genesis_sst_100015_19820313.nc` (33,551 bytes). Both have the seven exact 03Z–21Z timestamps and area [7.5, 152.0, -1.0, 163.0]; pressure has u/v/t/q/w with units and 850/700/500/200 hPa levels, SST has `sst` in K. The canonical journal records both as `validated` with byte sizes. The initial process was still holding the lock while accepted; a second invocation was correctly rejected, and the original process completed both jobs without a duplicate submission.
- **Live acquisition checkpoint (2026-09-13):** 32 requests are validated, totaling 5,113,015 bytes in `data/raw/era5/genesis_smoke/v1`; 34 CDS job IDs are recorded, with two requests awaiting recovery after client read timeouts. Two requests have submission errors and no IDs: `genesis_pressure_100048_19820726.nc` (connection reset; fingerprint `c156d74455e6152e03744ca8a9b766df1b452e033890719433d8b210c6fe83af`) and `genesis_sst_100049_19820728.nc` (read timeout; fingerprint `dcd089eabfe912696db904cb16e18f4d8840d69ef2926d2e818c31e001394dd7`). Both remain unresolved and must not be looked up or resubmitted automatically. The runner stopped at the second ambiguous submission and released the lock. Manifest SHA-256 remains `9a4add829832a428049c6f8e75d9a70a40fdff5bec05783033f28f9ca19ff4e3`.
- Genesis skip-and-continue behavior has a focused resume test. No full acquisition was run after this code change; resume only when requested, using the same manifest and raw directory.
